//add a question

export const QuestionForm = () => {
    return <h1>THIS IS FOR EDITING</h1>
}


{/* <button style={{background:"#D1483F"}} onClick={(evt) => {
          evt.preventDefault()
           const questionData = {
             ...question,
             label: post.label,
             solution: solutionsForQuestion,
           }
           updateQuestion(questionId, questionData).then(() => {
             navigate(`/questions`)
           })
        }}>Unapprove Post</button></> */}